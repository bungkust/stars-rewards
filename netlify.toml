[build]
  # Deploy docs folder (landing page, privacy, terms) to root domain
  # React app should NOT be accessible from web - mobile only
  publish = "docs"
  command = "echo 'Deploying docs folder only - no build needed'"

# No SPA fallback needed for static docs
# Remove all React app redirects

[build.environment]
  NODE_VERSION = "18"